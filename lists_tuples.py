# Tuples are immutable lists, using () instead of []
dimensions = (10, 100)
print(dimensions)
print(dimensions[0])

# Tuples with just 1 value are defined with a presence of a comma, not just ()
dimensions = (10,)
print(dimensions)
